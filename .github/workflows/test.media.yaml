name: Test - Media

on:
  pull_request:
    paths:
      - .github/workflows/test.media.yaml
      - configs/eslint/**
      - configs/stylelint/**
      - configs/ts/**
      - configs/vite/**
      - package.json
      - pnpm-lock.yaml
      - pnpm-workspace.yaml
      - turbo.json
      - x/media/**
  push:
    branches:
      - main
      - rc
    paths:
      - .github/workflows/test.media.yaml
      - configs/eslint/**
      - configs/stylelint/**
      - configs/ts/**
      - configs/vite/**
      - package.json
      - pnpm-lock.yaml
      - pnpm-workspace.yaml
      - turbo.json
      - x/media/**
  workflow_dispatch:

jobs:
  test:
    name: Test (ubuntu-latest)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up pnpm
        uses: pnpm/action-setup@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: package.json
          cache: pnpm

      - name: Install Dependencies
        run: pnpm install

      - name: Lint
        run: pnpm lint:media

      - name: Type Check
        run: pnpm check-types:media
